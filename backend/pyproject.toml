[project]
name = "pm-backend"
version = "0.1.0"
description = "FastAPI backend for the Project Management MVP"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.115.8",
  "httpx==0.28.1",
  "pytest==8.3.4",
  "pytest-cov==5.0.0",
  "uvicorn==0.34.0",
]

[tool.uv]
package = false
